server:
  port: 20000

spring:
  application:
    name: tetto-auth-service
  cloud:
    nacos:
      config:
        server-addr: 192.168.31.101:8848 #配置中心地址
        file-extension: yaml #指定yaml格式的配置
        group: DEV_GROUP

#  session:
#    store-type: redis
#    timeout: 30m