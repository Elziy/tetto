server:
  port: 30000

spring:
  application:
    name: tetto-third-party-service
  cloud:
    nacos:
      config:
        server-addr: 43.143.223.5:8848 #配置中心地址
        file-extension: yaml #指定yaml格式的配置
        group: DEV_GROUP